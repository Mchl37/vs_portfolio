import styles from "../styles/page.module.scss";

export default function Home() {
  /* return (
    <main className={ styles.main }>
      <div className={ styles.container }>
        <div className={ styles.topBar }>
          <div className={ styles.squareButtons }>
            <div className={ styles.redSquareButton }></div>
            <div className={ styles.yellowSquareButton }></div>
            <div className={ styles.greenSquareButton }></div>
          </div>
          <h1 className={ alice.className }>Visuale Studio Codex <span>(c&apos;est du latin)</span></h1>
        </div>
        <div className={ styles.leftBar }></div>
        <div className={ styles.filesBar }></div>
        <div className={ styles.tabsBar }></div>
        <div className={ styles.header }></div>
        <div className={ styles.content }></div>
        <div className={ styles.botBar }>
          <p>Créé et développé par Michel GUELIN © 2024</p>
        </div>
      </div>
    </main>
  ); */
}
